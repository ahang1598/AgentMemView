{
  "name": "@agentmemview/cli",
  "version": "0.1.0",
  "description": "AgentMemView CLI: init/start/stop/open/export/import/doctor",
  "type": "module",
  "license": "MIT",
  "bin": {
    "agentmemview": "./bin/agentmemview.js"
  },
  "files": [
    "bin",
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@agentmemview/core": "workspace:*",
    "@agentmemview/proxy": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0"
  }
}
